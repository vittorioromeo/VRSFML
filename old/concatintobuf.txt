 char        buf[512];
    base::SizeT nextBufPos = 0u;

    const auto concatIntoBuf = [&](const char* const str)
    {
        const base::SizeT len = ZA_STRLEN(str);
        if (nextBufPos + len + 1u < sizeof(buf))
        {
            ZA_STRNCPY(buf + nextBufPos, str, len + 1u);
            nextBufPos += len;
        }
    };
